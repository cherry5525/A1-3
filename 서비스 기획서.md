
# 📋 MealGenie 서비스 기획서

## 1. 서비스 개요
- **서비스명**: MealGenie (밀지니)
- **한 줄 소개**: 성별·나이·목적에 맞는 개인 맞춤 식단을 AI가 추천해주는 웹 서비스
- **목적**:
  - 개인별 특성을 반영한 맞춤 식단 제공
  - 식단 고민 시간을 줄이고, 지속 가능한 식습관 형성을 도움

## 2. 타겟 사용자
- 다이어트·근육 증가 등 목적을 가지고 식단을 관리하고 싶은 사람
- 매번 무엇을 먹어야 할지 고민하는 사람
- 자신에게 맞는 식단을 간편하게 추천받고 싶은 사람

## 3. 페이지 / 섹션 구성
> 네비게이션 방식: **상단 고정 메뉴 + 한 페이지 스크롤(앵커 이동)**

| 섹션 | 내용 |
|------|------|
| 메인 (소개) | 서비스 소개, 특징 안내, "지금 시작하기" 버튼 |
| AI 추천 | 8개 입력 폼 → AI 추천 식단 결과 표시 (**핵심 기능**) |
| FAQ | 자주 묻는 질문 (알레르기 반영, 결과 저장, 비용 등) |

## 4. 핵심 기능 (AI 식단 추천)

### 입력 (Input)
| 항목 | 필수 여부 |
|------|-----------|
| 성별 | 필수 |
| 나이 | **필수 (검증 대상)** |
| 기간(일) | **필수 (검증 대상)** |
| 목적 (다이어트/근육 증가/건강 유지) | 필수 |
| 좋아하는 음식 | 선택 |
| 알레르기 | 선택 |
| 키(cm) | 선택 |
| 몸무게(kg) | 선택 |

### 처리 로직
1. 프론트에서 입력값을 수집 후 `fetch('/api/recommend')`로 POST 요청
2. 백엔드(Flask)에서 입력값을 받아 프롬프트 조립
   - 키·몸무게 입력 시 **BMI 자동 계산** 후 체중 상태에 맞는 안내 반영
   - 알레르기 입력 시 해당 재료 제외 문구 추가
3. OpenAI GPT(gpt-5-mini)에 프롬프트 전달
4. AI 응답을 JSON으로 프론트에 반환하여 화면에 표시

### 출력 (Output)
- 사용자 조건에 맞는 **맞춤 식단 추천 텍스트**
- 결과 아래에 **복사하기 / PDF 저장** 버튼 제공

### 사용자 제공 가치
- 개인 특성 기반의 맞춤 식단으로 식단 고민 해소
- 결과 저장(PDF/복사)으로 실생활 활용 가능

## 5. 실패 처리 기준
| 상황 | 처리 방식 |
|------|-----------|
| **빈 입력 (필수값 누락)** | 나이·기간 미입력 시 → "⚠️ 나이와 기간을 입력해주세요!" 토스트 안내 후 요청 중단 |
| **API / 서버 오류 (4xx/5xx)** | `response.ok` 확인 → 실패 시 "😢 추천을 받지 못했어요. 잠시 후 다시 시도해주세요." 안내 |
| **처리 중 상태 표시** | 요청 중 "식단을 만들고 있어요..." 스피너 표시 |

## 6. 추가 구현 (UX 고도화 - 보너스)
- 🌙 **다크 모드**: 테마 전환 및 localStorage 저장
- 💾 **입력값 자동 저장/복원**: 재방문 시 이전 입력값 유지
- 📋 **결과 복사** / 📕 **PDF 저장** 기능
- 📱 **반응형 레이아웃**: 모바일/태블릿/데스크톱 대응

## 7. AI 기능 테스트 케이스
- **정상 입력**: "여성 / 25살 / 7일 / 다이어트 / 샐러드" → 맞춤 식단 결과 표시
- **빈 입력**: 나이·기간 미입력 → "⚠️ 나이와 기간을 입력해주세요!" 안내
- **오류 상황**: 서버 응답 실패 → "😢 추천을 받지 못했어요..." 안내